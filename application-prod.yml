spring:
  application:
    name: Config-Prod
    
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    resister-with-client: true
    service-url:
      defaultZone: http://localhost:8761/eureka
